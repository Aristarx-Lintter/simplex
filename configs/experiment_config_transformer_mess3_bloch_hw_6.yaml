global_config:
  output_dir: "./results"
  device: cuda
  parallel: true
  wandb: false
  wandb_project: "quantum_transformer"
  process_dir: "/workspace-SR008.nfs2/nachevsky/simplex/epsilon-transformers/process_data" 
  val_every: 1 # after n epochs, ~ is None
  scheduler: true
  save_every: 100

train_config:
  batches_per_epoch: 400
  bos: false
  n_epochs: 2000

model_config:
  n_ctx: 6
  act_fn: "relu"
  normalization_type: "LN"
  attn_only: false
  seed: 42
  dtype: float32

sweep_config:
  train_config:
    learning_rate:
      - 1.0e-4
    batch_size:
      - 1024
  model_config:
    d_head:
      - 16
    n_layers:
      - 4
    n_heads:
      - 4
  process_config:
    - name: "mixture"
      processes:
      - name: "tom_quantum"
        alpha: 1.0
        beta: 7.14142842854285
      - name: "mess3"
        x: 0.05
        a: 0.85